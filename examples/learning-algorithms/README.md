# Comparison of different learning methods for Edge SNNs

 - For backpropogation through time (BPTT), run:
    ```

    ```
 - For temporal learning backpropagation (TLBP), run:
    ```
    python tsbp main.py -config Networks/[config_filename].yaml
    ```

